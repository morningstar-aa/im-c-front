#ifndef NIM_P2P_SDK_PROC_PROG_DEF_H
#define NIM_P2P_SDK_PROC_PROG_DEF_H

// Struct for process progress registration (Recreated as dummy if structure is unknown)
struct TransferFileProcProgRegInfo
{
    // Unknown fields, leaving empty for now or adding placeholder if needed.
    // Assuming it's a POD struct.
    int placeholder; 
};

#endif // NIM_P2P_SDK_PROC_PROG_DEF_H
